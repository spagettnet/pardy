node_modules
dist
.env
.env.local
*.log
.DS_Store

# Generated/ingested data — rebuild via pnpm data:build
data/raw/
data/episodes.json
data/categories.json

# Self-signed TLS cert — regenerated via pnpm cert
certs/

# Python venv + caches for the vendored TTS/STT services
.venv/
__pycache__/
.kokoro-models/
# (uv.lock IS committed for reproducibility)
